# Legacy / dokumentacja.
# Produkcyjna konfiguracja Alertmanagera powstaje w:
#   scripts/install-monitoring-lite.sh
# z pliku .secrets/ses-smtp.env (SMTP user/hasło SES).
#
# Alerty → a.artamonov@wp.pl
# From: artamonovandrei88@gmail.com (zweryfikowana tożsamość SES)
#
# UI: http://<k3s-ip>:30903
# Prometheus Alerts: http://<k3s-ip>:30090/alerts
